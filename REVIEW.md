- [blocker] Add a `LICENSE` file (MIT).
- [high] Align fallback warning behavior with docs: `fallback_warn = "once"` should be once-per-buffer/session, or docs should be corrected.
- [high] Guard jump navigation against stale line numbers by clamping target lines before `nvim_win_set_cursor`.
- [high] Refresh active buffers after edits (`TextChanged`, `TextChangedI`) with light debouncing to keep `path_order` current.
- [high] Reduce dynamic mode LSP pressure by debouncing retargeting.
- [high] Consider async LSP highlight requests in dynamic mode to reduce sync request load.
- [high] Clarify `:TunnelVisionMode toggle` semantics (currently `static <-> flow`) or implement full cycle including `dynamic`.
- [high] Clarify `scope` semantics (`auto` vs `function`) by differentiating behavior or documenting current equivalence (is it even needed considering we now have lsp as default and lexical as fallback? I don't see how this make sense).
- [high] Add `pcall` protection around vulnerable `vim.api` calls in `core.lua` and `ui.lua` to avoid invalid buffer/window race errors.
- [high] Prevent repeated warning resets on deactivate if the intended behavior is once-per-buffer/session.
- [api] Add type validation in `set_renderer` to reject non-function values.
- [api] Add `toggle` support to `set_symbol_source` for setter API consistency.
- [api] Remove unnecessary `local next_source = source` indirection in `set_symbol_source` if no toggle path is used.
- [api] Keep command creation one-time, but make keymap setup resilient to in-session reconfiguration or clearly document setup-time-only behavior.
- [api] Add a note that `assign_ops` ordering matters and `=` must remain last.
- [api] Add rationale for the `guard < 32` flow propagation cap and consider making it configurable.
- [api] Document known limitations of `strip_strings_and_comments` (escaped quotes, multiline strings, block comments, `#` semantics).
- [api] Consider externalizing the `keywords` table for maintainability.
- [ui] Avoid overwriting user-defined dim highlight settings unless explicitly requested.
- [ui] Make dim extmark priority configurable (for example, `dim_priority`) instead of fixed `1000`.
- [perf] Optimize dim rendering for large buffers (current full-buffer extmark pass can cause jank near `max_dim_lines`).
- [perf] Consider window-range dimming and/or incremental dim updates.
- [perf] Avoid unnecessary double buffer reads across compute and dim phases.
- [docs] Fix README wording and grammar (for example, "one symbol at a time").
- [docs] Add a minimal usage walkthrough (open file -> activate -> navigate -> deactivate).
- [docs] Fully document all config options and defaults, especially currently under-documented options.
- [docs] Document `TunnelVisionDim` behavior, customization, and colorscheme reapplication.
- [docs] Document the full public Lua API (`setup`, `toggle`, `forward`, `dynamic`, `next`, `prev`, `refresh`, getters/setters).
- [docs] Add a Troubleshooting/FAQ section (fallback warnings, flow noise, activation issues, dim customization).
- [docs] Update command wording so `:TunnelVisionNext` and `:TunnelVisionPrev` clearly describe path-line jumps.
- [docs] Add LuaCATS docs (`---`, `@param`, `@return`) for public API in `init.lua`, `core.lua`, and `ui.lua`.
- [docs] Add badges for Neovim version requirement and license.
- [docs] Optionally add a visual demo (GIF).
- [docs] Optionally add a Contributing section and link contributor docs.
- [repo] Add `.gitignore` with Lua/Neovim essentials (swap files, rocks artifacts, tags, etc.).
- [repo] Add `CHANGELOG.md` with an initial release entry.
- [repo] Decide whether to remove stale internal planning docs (`improve_plugin.md`, `reviewing.md`, `refactor_plan.md`) from release package.
- [repo] If internal docs are kept, reconcile outdated references and inconsistencies.
- [repo] Address empty `plugin/` directory (add loader if needed, or remove directory).
- [repo] Add `:checkhealth` integration in `lua/tunnelvision/health.lua` (Neovim version, Tree-sitter, LSP, highlight checks).
- [repo] Add Neovim help docs (`doc/tunnelvision.txt`) for `:help tunnelvision` discoverability.
- [repo] Optionally add `CONTRIBUTING.md`.
- [tooling] Add formatter/lint configuration (`stylua.toml`, `.luacheckrc`, or `selene.toml`).
- [tooling] Add minimal CI in `.github/workflows/ci.yml` with format/lint and headless plugin-load smoke checks.
- [tooling] Optionally run CI on Neovim stable and nightly.
- [tooling] Add a minimal test/smoke harness for setup, command registration, and activation path.
- [verify] Run `luac -p` syntax checks before release.
- [verify] Run Neovim smoke tests across modes and key commands before release.
- [verify] Validate colorscheme switching behavior for dim highlights.
- [verify] Do a final README vs behavior consistency pass.

